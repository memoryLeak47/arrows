# c/MessageDispatcher

ApproxDuration: ""
DependsOn: "c/MessageSystem"

# Description
class MessageListener;

MessageListener has applyMessage(const <Name>Message& message)-Function for every MessageType.
If an Object tries to differ between Messages, he derives from MessageListener.

Every Message has a apply(MessageListener* listener)-Function. This Message::apply-Function calls listener.applyMessage(*this).

# Rating - Prediction
# Rating - Review
