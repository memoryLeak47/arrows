package network.game.packets;

import network.Packet;
import misc.math.Body;

public class ImagePacket extends Packet // contains Images, sent from server to client in game phase
{
	private Body body;
}
